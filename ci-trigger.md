## CI Trigger File

This file exists solely to trigger the Android GitHub Actions workflow when updated and pushed to the `master` branch.

You can edit this file (for example, by appending a new timestamp line) and push the change to `master` to initiate a new CI build without modifying any app code or build configuration.

